% ============================================================
%  template.tex — Plantilla pandoc del informe técnico UCuenca
%  Módulo 1217 · Redes Complejas
%  Generado a partir de Informe.md vía:
%    pandoc Informe.md -o informe.pdf --template=template.tex --pdf-engine=xelatex
% ============================================================
\documentclass[10pt, a4paper]{article}

% ============================================================
% Codificación y lenguaje
% ============================================================
\usepackage{fontspec}
\usepackage[spanish]{babel}
\decimalpoint

% DejaVu tiene mucha mejor cobertura Unicode que Latin Modern (los
% símbolos ✓ ≈ ⚠ ≥ ≤ ↔ del informe, generados por scripts/markdown,
% no existen en Latin Modern y se perdían silenciosamente).
\setmainfont{DejaVu Serif}
\setsansfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}

% DejaVu no trae ⚠ ✓ ✗ — fallback puntual a Segoe UI Symbol (Windows)
% solo para esos tres glifos, sin cambiar la fuente del resto del texto.
\usepackage{newunicodechar}
\newfontfamily\symbolfont{Segoe UI Symbol}
\newunicodechar{✓}{{\symbolfont ✓}}
\newunicodechar{✗}{{\symbolfont ✗}}
\newunicodechar{⚠}{{\symbolfont ⚠}}

% ============================================================
% Geometría de página
% ============================================================
\usepackage[
    a4paper,
    left   = 1.7cm,
    right  = 1.7cm,
    top    = 1.7cm,
    bottom = 1.8cm
]{geometry}

% Interlineado ligeramente reducido (imperceptible a la lectura, ahorra
% volumen acumulado en 30+ páginas de texto denso).
\linespread{0.94}

% ============================================================
% Matemáticas
% ============================================================
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{bm}

% ============================================================
% Gráficos y figuras
% ============================================================
\usepackage{graphicx}
\usepackage{float}
\let\origfigure\figure
\let\endorigfigure\endfigure
\renewenvironment{figure}[1][2] {
    \expandafter\origfigure\expandafter[H]
} {
    \endorigfigure
}
\setlength{\parindent}{0pt}

% Pandoc necesita esto para \tightlist
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Imágenes no más anchas que la página (pandoc >=3 envuelve cada imagen
% en \pandocbounded{...}; si el template no la define, falla con
% "Undefined control sequence" en cada figura).
\usepackage{grffile}
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{%
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@
    \let\@tempa\@tempb
  \fi
  \ifdim\@tempa\p@<\p@
    \scalebox{\@tempa}{\usebox\pandoc@box}%
  \else
    \usebox\pandoc@box
  \fi
}
\makeatother

% ============================================================
% Colores
% ============================================================
\usepackage{xcolor}
\definecolor{azulsec}{RGB}{26, 88, 140}
\definecolor{grisfondo}{RGB}{245, 245, 245}
\definecolor{borde}{RGB}{70, 70, 70}
\definecolor{tabhead}{RGB}{30, 70, 115}
\definecolor{tabfila}{RGB}{235, 241, 248}

% ============================================================
% Tablas (longtable ANTES de caption: caption detecta el tipo
% "table" que define longtable vía \LTcaptype; en el orden
% inverso hyperref intenta anclar un contador "none" inexistente
% y longtable falla con "No counter 'none' defined")
% ============================================================
\usepackage{calc}
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{array}
\usepackage{longtable}
\usepackage{multirow}
\renewcommand{\arraystretch}{1.1}
% Todas las tablas (longtable) en tamaño small para compactar el
% volumen de las ~30 tablas de resultados sin perder legibilidad.
\let\oldlongtable\longtable
\let\endoldlongtable\endlongtable
\renewenvironment{longtable}{\small\oldlongtable}{\endoldlongtable}

% Pandoc emite \def\LTcaptype{none} en tablas sin \caption, para que
% longtable no incremente el contador "table". El paquete caption (con
% singlelinecheck=false más abajo) intenta resolver "none" como si fuera
% un contador real y falla con "No counter 'none' defined". Se define un
% contador ficticio "none" para que ambos paquetes queden contentos sin
% que las tablas sin caption numeren nada real.
\newcounter{none}

% ============================================================
% Captions
% ============================================================
\usepackage{caption}
\captionsetup{
    font        = footnotesize,
    labelfont   = bf,
    labelsep    = period,
    justification = justified,
    singlelinecheck = false,
    skip        = 3pt
}
\captionsetup[figure]{name = Fig., justification = centering, aboveskip=2pt, belowskip=0pt}
\captionsetup[table]{name = Tabla, position = above, justification = centering, belowskip=1pt}

% Espaciado vertical alrededor de figuras/tablas flotantes más apretado
\setlength{\textfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{5pt plus 2pt minus 2pt}
\setlength{\floatsep}{5pt plus 2pt minus 2pt}

% ============================================================
% Formato de secciones
% ============================================================
\usepackage{titlesec}
\titleformat{\section}
    {\color{azulsec}\large\bfseries}
    {\thesection.}
    {0.5em}
    {}
    [\color{azulsec}\vspace{2pt}\rule{\linewidth}{0.7pt}]
\titleformat{\subsection}
    {\color{azulsec}\normalsize\bfseries}
    {\thesubsection}
    {0.5em}
    {}
\titleformat{\subsubsection}
    {\color{azulsec}\normalsize\bfseries\itshape}
    {\thesubsubsection}
    {0.5em}
    {}
\titleformat{\paragraph}[runin]
    {\color{azulsec}\normalsize\bfseries}
    {}
    {0em}
    {}
\titlespacing{\section}    {0pt}{9pt}{4pt}
\titlespacing{\subsection} {0pt}{6pt}{2pt}
\titlespacing{\subsubsection}{0pt}{5pt}{1pt}
\titlespacing{\paragraph}{0pt}{4pt}{3pt}

% ============================================================
% Código
% ============================================================
\usepackage{fancyvrb}
\newcommand{\VerbBar}{|}
\newcommand{\VERB}{\Verb[commandchars=\\\{\}]}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\}}
\usepackage{framed}
\definecolor{shadecolor}{RGB}{245,245,245}
\newenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}
\newcommand{\AlertTok}[1]{#1}
\newcommand{\AnnotationTok}[1]{#1}
\newcommand{\AttributeTok}[1]{#1}
\newcommand{\BaseNTok}[1]{#1}
\newcommand{\BuiltInTok}[1]{#1}
\newcommand{\CharTok}[1]{#1}
\newcommand{\CommentTok}[1]{\textit{#1}}
\newcommand{\CommentVarTok}[1]{\textit{#1}}
\newcommand{\ConstantTok}[1]{#1}
\newcommand{\ControlFlowTok}[1]{\textbf{#1}}
\newcommand{\DataTypeTok}[1]{#1}
\newcommand{\DecValTok}[1]{#1}
\newcommand{\DocumentationTok}[1]{\textit{#1}}
\newcommand{\ErrorTok}[1]{#1}
\newcommand{\ExtensionTok}[1]{#1}
\newcommand{\FloatTok}[1]{#1}
\newcommand{\FunctionTok}[1]{#1}
\newcommand{\ImportTok}[1]{#1}
\newcommand{\InformationTok}[1]{#1}
\newcommand{\KeywordTok}[1]{\textbf{#1}}
\newcommand{\NormalTok}[1]{#1}
\newcommand{\OperatorTok}[1]{#1}
\newcommand{\OtherTok}[1]{#1}
\newcommand{\PreprocessorTok}[1]{#1}
\newcommand{\RegionMarkerTok}[1]{#1}
\newcommand{\SpecialCharTok}[1]{#1}
\newcommand{\SpecialStringTok}[1]{#1}
\newcommand{\StringTok}[1]{#1}
\newcommand{\VariableTok}[1]{#1}
\newcommand{\VerbatimStringTok}[1]{#1}
\newcommand{\WarningTok}[1]{#1}

\newcommand{\code}[1]{\texttt{\small #1}}

% ============================================================
% Listas
% ============================================================
\usepackage{enumitem}
\setlist[itemize]{leftmargin = 1.3em, itemsep = 1pt, topsep = 2pt}
\setlist[enumerate]{leftmargin = 1.3em, itemsep = 1pt, topsep = 2pt}

\setlength{\parskip}{2pt}

% ============================================================
% Encabezado y pie de página
% ============================================================
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\small\thepage}

% ============================================================
% Hipervínculos
% ============================================================
\usepackage[unicode=true]{hyperref}
\hypersetup{
    colorlinks  = true,
    linkcolor   = azulsec,
    citecolor   = azulsec,
    urlcolor    = azulsec,
    pdftitle    = {$title$},
    pdfauthor   = {$author$},
    pdfsubject  = {Redes Complejas},
    pdfkeywords = {redes complejas, UCuenca, percolacion, flujo maximo}
}
\urlstyle{same}

% Evita que las tablas largas salten de sección y respeta anchos
\usepackage{tabularx}

\newcommand{\autorsep}{\quad\textperiodcentered\quad}
\newcommand{\prom}[1]{\langle #1 \rangle}
\newcommand{\norm}[1]{\left\| #1 \right\|}

% Referencias cruzadas de pandoc por sección
\providecommand{\subtitle}[1]{}

\begin{document}

% ------------------------------------------------------------
% PORTADA
% ------------------------------------------------------------
\begin{center}
    {\LARGE\bfseries $title$ \par}
    \vspace{0.5em}
    $if(subtitle)$
    {\normalsize $subtitle$ \par}
    \vspace{0.5em}
    $endif$
    {\normalsize $for(author)$$author$$sep$ \autorsep $endfor$ \par}
    \vspace{0.3em}
    {\small Universidad de Cuenca \enspace---\enspace $date$ \par}
\end{center}

\vspace{0.4em}

$if(abstract)$
\begin{quote}
\textbf{Resumen---} $abstract$
\end{quote}
$endif$

$body$

\end{document}
